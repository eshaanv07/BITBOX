--after downloading run these commands

curl -fsSL https://deno.land/install.sh | sh

echo 'export PATH="$HOME/.deno/bin:$PATH"' >> ~/.bashrc

source ~/.bashrc

deno --version   # confirm it worked

--make sure the code files are in a folder named BITBOX or change the code yourself.
